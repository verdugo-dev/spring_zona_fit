services: 
  db:
    image: mysql:8.0
    container_name: zona_fit_db
    restart: always
    environment:
      MYSQL_DATABASE: zona_fit_db
      MYSQL_ROOT_PASSWORD: admin
    ports:
      - "3306:3306"
    volumes:
      - zona_fit_db_data:/var/lib/mysql

volumes:
  zona_fit_db_data:
